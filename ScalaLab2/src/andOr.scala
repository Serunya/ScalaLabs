object andOr {
  def and(a:Boolean,b: Boolean): Boolean ={
    if(a & b){
      return true
    }
    return false
  }

  def or(a:Boolean,b: Boolean): Boolean ={
    if(a | b){
      return true
    }
    return  false
  }
}
